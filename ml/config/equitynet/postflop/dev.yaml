board_clustering:
  n_clusters_limit: 6
  boards_per_cluster: 2

equity_mc:
  postflop_samples: 30
  max_resample: 20

train:
  batch_size: 1024
  max_epochs: 6
  patience: 2
  checkpoints_dir: checkpoints/equity_post/dev.yaml